# General text file normalization
* text=auto eol=lf

# Windows scripts
*.bat text eol=crlf
*.cmd text eol=crlf

# Git LFS for large binary types
*.psd filter=lfs diff=lfs merge=lfs -text
*.fbx filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text

# Unity text-based assets (ensure proper diffs and line endings)
*.cs text diff=csharp
*.shader text
*.meta text
*.unity text
*.prefab text
*.mat text
